┌──────────────────────────────────────────────────────────────────────────────────────┐
│                          BOSS-RECRUITMENT-SYSTEM 系统架构                              │
│                                                                                      │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐                         │
│  │   F5     │   │   F6     │   │   F7     │   │ Dashboard│                         │
│  │ 主动打招呼│   │ 简历采集 │   │ 批量AI回复│   │ 数据总控 │                         │
│  └────┬─────┘   └────┬─────┘   └────┬─────┘   └────┬─────┘                         │
│       │              │              │              │                                  │
│  ┌────┴──────────────────────────────────────────────────┴────┐                      │
│  │                       FastAPI REST (api.py)                 │                      │
│  │  /api/filter/contact  /api/resume/batch  /api/workflow/reply│                      │
│  │  /api/stats  /api/browser/*  /api/chat/*  /api/auth/*       │                      │
│  └────┬──────────────┬──────────────┬──────────────┬───────────┘                      │
│       │              │              │              │                                  │
│  ┌────┴────┐   ┌─────┴─────┐  ┌─────┴─────┐  ┌────┴─────┐                            │
│  │  JWT    │   │ filter_   │  │  chat_    │  │  chat_   │                            │
│  │  auth   │   │ criteria  │  │  service  │  │  stage   │                            │
│  │ 鉴权模块 │   │ 筛选条件  │  │ AI回复生成│  │ 阶段推算 │                            │
│  └─────────┘   └───────────┘  └─────┬─────┘  └──────────┘                            │
│                                     │                                               │
│                              ┌──────┴──────┐                                        │
│                              │  DeepSeek   │                                        │
│                              │  AI API     │                                        │
│                              │ deepseek-chat│                                       │
│                              └─────────────┘                                        │
│                                                                                      │
│  ┌─────────────────────────────────────────────────────────────────────────────┐     │
│  │                         Browser Automation (automation.py)                   │     │
│  │                                                                             │     │
│  │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐                   │     │
│  │  │   nodriver   │    │   xdotool    │    │  CDP click   │                   │     │
│  │  │  CDP 连接    │    │  贝塞尔鼠标  │    │  原生点击    │                   │     │
│  │  │  Chrome复用  │    │  Enter/滚动  │    │  Input事件   │                   │     │
│  │  └──────────────┘    └──────────────┘    └──────────────┘                   │     │
│  │                                                                             │     │
│  │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐                   │     │
│  │  │  CDP下载拦截 │    │  Cookie持久化│    │  Session     │                   │     │
│  │  │  简历自动保存│    │  导入/导出   │    │  健康检查    │                   │     │
│  │  └──────────────┘    └──────────────┘    └──────────────┘                   │     │
│  └─────────────────────────────────────────────────────────────────────────────┘     │
│                                                                                      │
│  ┌──────────────────────────────────────────────────────┐                            │
│  │                  chat_nav (导航层)                    │                            │
│  │                                                      │                            │
│  │  navigate_to_chat    click_contact    get_contacts   │                            │
│  │  get_messages        type_and_send    check_limit    │                            │
│  │  click_unread_filter click_communicating_filter      │                            │
│  └──────────────────────────────────────────────────────┘                            │
│                                                                                      │
│  ┌────────────────┐   ┌────────────┐   ┌──────────────┐                             │
│  │   SQLite DB    │   │  Docker    │   │  VNC + noVNC │                             │
│  │                │   │            │   │              │                             │
│  │ candidates     │   │ FastAPI    │   │  :5901 VNC   │                             │
│  │ conversations  │   │ Chrome     │   │  :6901 Web   │                             │
│  │ contact_records│   │ Chromium   │   │  远程桌面    │                             │
│  │ chat_sessions  │   │ XFCE桌面   │   │  BOSS扫码    │                             │
│  │ resume_ops     │   │ 共享内存2G │   │              │                             │
│  │ reply_templates│   │            │   │              │                             │
│  └────────────────┘   └────────────┘   └──────────────┘                             │
│                                                                                      │
│  ┌─────────────────────────────────────────────────────────────────────────────┐     │
│  │                          三大工作流详细流程                                    │     │
│  │                                                                             │     │
│  │  F5 主动打招呼                    F6 简历采集                 F7 批量AI回复    │     │
│  │  ────────────                     ──────────                  ─────────────    │     │
│  │  ① 导航推荐牛人页                 ① 导航聊天页→点"沟通中"    ① 导航聊天页     │     │
│  │  ② 提取iframe内候选人卡片         ② 滚动列表→逐次查找联系人   ② 点"未读"筛选  │     │
│  │  ③ 学校/学历/年限筛选             ③ 点击联系人→验证面板切换   ③ 点击联系人     │     │
│  │  ④ 去重(seen+contacted_today)    ④ 找附件简历>在线简历按钮   ④ 提取聊天消息    │     │
│  │  ⑤ CDP点击打招呼按钮             ⑤ 点弹窗上附件入口          ⑤ 过滤UI噪音     │     │
│  │  ⑥ DB写入+每日上限检查           ⑥ 检测case:                 ⑥ 构建LLM上下文  │     │
│  │  ⑦ 滚动加载下一批                 │  ├ pdf_preview → 下载     ⑦ DeepSeek生成   │     │
│  │  ⑧ 限流弹窗检测                    │  ├ request_popup → 确认   ⑧ 冗余检测       │     │
│  │  ⑨ 每日上限:80 本次上限:20        │  ├ request_pending→跳过   ⑨ type_and_send │     │
│  │                                   │  ├ need_reply → 跳过      ⑩ Enter发送     │     │
│  │                                   │  └ online_resume→截图     ⑪ DB保存        │     │
│  │                                   │  ⑦ CDP下载拦截+轮询确认   ⑫ 间隔2-5s      │     │
│  │                                   │  ⑧ candidates表更新                         │     │
│  │                                   │  ⑨ Escape×3关闭弹窗                         │     │
│  └─────────────────────────────────────────────────────────────────────────────┘     │
│                                                                                      │
│  ┌───────────────────────┐   ┌──────────────────────────┐                            │
│  │   公司/岗位背景注入     │   │   system_prompt 模板     │                            │
│  │   job_info/            │   │                          │                            │
│  │   company_profile.txt  │   │   "你是一名专业的招聘官"  │                            │
│  │   (Docker volume挂载)  │   │   严禁索要微信/电话       │                            │
│  └───────────────────────┘   │   回复简洁自然≤80字       │                            │
│                              └──────────────────────────┘                            │
└──────────────────────────────────────────────────────────────────────────────────────┘
